import RenderResume from "@/components/render-resume";

export const metadata = {
  title: "Resume",
  description: "Look at resume.",
};

export default function Resume() {
  return <RenderResume />;
}
